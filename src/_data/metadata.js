'use strict';

const config = require('../config');

const baseUrl = config.get('baseUrl');

module.exports = {
  title: 'Immersve',
  url: `${baseUrl}/`,
  description: 'World\'s first multichain payment protocol',
  site_name: 'Immersve',
  img: `${baseUrl}/img/previewlogo-5bfcbf3ec91bbff300fb474bdd983123.png`,
};
