'use strict';

const config = require('../config');

const baseUrl = config.get('baseUrl');

module.exports = {
  title: 'Immersve',
  url: `${baseUrl}/`,
  description: 'World\'s first multichain payment protocol',
  site_name: 'Immersve',
  img: `${baseUrl}/img/previewlogo-be46f42385bfb0e5abe0918800523884.png`,
};
